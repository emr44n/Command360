# 29 · Marketing Site Content Update

*Content refresh only — NO design change. The v5 regimental-grid design system stays exactly as is (file 15). This updates the words/sections so the public site reflects what the product actually does now.*

## Pages to update (all exist in the repo)
`/` (home) · `/product` · `/command-studio` · `/about` · `/solutions` · `/pricing` (copy only if bands changed) · `/help` (feature explanations).

## What the copy must now reflect (from files 06–13, 22, 26–28)
- **Command Classroom:** interactive TDE decks — polls, quizzes, word clouds, Q&A, surveys, decision capture; **slide masters** (your branding on every slide); **video on slides** with feather/mask/borders; the **news ticker** lower-third; QR/code join with **no participant accounts** (privacy advantage — say it).
- **Command Studio:** the 2D scene builder — layers, blend modes, feathering, masks, CCTV multi-view; the **effects library: fire, smoke, explosions**, water, hazmat, lighting; characters/vehicles/props; timeline + events; **export scenes as MP4**.
- **Classroom Scene (the bridge):** build a scene in Studio, run it in a lesson **click-by-click like PowerPoint** — smoke, then flames, then an explosion, jumping between perspective views and CCTV feeds live in front of the class.
- **Command Live + Debrief:** code-based live sessions, injects, multiviewer/second screen, decision logs and **evidence export** for regulators.
- **About:** founder story stays; capabilities paragraph updated to the above.

## Rules
- British English; plain, confident, operational tone (write for incident commanders, not tech buyers). No invented capabilities — copy must match what's actually shipped at the time the page goes live (phase-gate this with the features).
- SEO: page titles/meta descriptions per page targeting buyer intent (digital TDEs, incident command training software, NOG/JESIP exercises — see GO_TO_MARKET file); proper h1/h2 structure; alt text on images.
- Where a feature ships later, the page describes the current release (no "coming soon" promises).

## Phasing
Ship the content update **in Phase 9** (once features are real), as a copy-only PR touching the marketing pages — verified against the live feature set.
